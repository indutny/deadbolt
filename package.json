{
  "name": "deadbolt",
  "description": "Autoreleasing locks for node.js",
  "tags": ["deadbolt", "locks", "weak"],
  "version": "0.3.2",

  "author": "Fedor Indutny <fedor@indutny.com>",
  "contributors": [
    { "name": "Fedor Indutny", "email": "fedor@indutny.com" }
  ],

  "main": "./lib/deadbolt",

  "dependencies": {
  },
  "devDependencies": {
    "vows": "0.5.x"
  },

  "scripts": {
    "install": "./configure && make",
    "test": "node --expose-gc `which vows` --spec test/*-test.js"
  }
}
