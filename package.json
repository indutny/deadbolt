{
  "name": "deadbolt",
  "version": "0.3.0",
  "main": "./lib/deadbolt",
  "dependencies": {
  },
  "devDependencies": {
    "vows": "0.5.x"
  },
  "scripts": {
    "install": "./configure && make",
    "test": "node --expose-gc `which vows` --spec test/*-test.js"
  }
}
